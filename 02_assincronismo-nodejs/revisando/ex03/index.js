const fetchPosts = () =>{
  return new Promise = ((resolve, reject) => {
    setTimeout(() => {
      const error = false;
      if(error) {
        reject(fudeo);
      }
      resolve("Ok");      
    }, 1000);
  });
};

const fetchComments = () =>{
  return new Promise = ((resolve, reject) => {
    setTimeout(() => {
      const error = false;
      if(error) {
        reject(fudeo2);
      }
      resolve("Ok");
    }, 1000);
  });
};

const fetch = async()=>{
  
};